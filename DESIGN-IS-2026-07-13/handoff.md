**Design Is Verdict:** REFINE — 26/30

**Top 3 moves to implement:**

1. Replace DeliveryMap SVG with a proper Karnataka outline + city pins (the current one doesn't read as Karnataka)
2. Add calculator validation: prevent negative numbers, show inline errors on dimension fields, success animation on quote sent
3. Add popular dimension quick-select chips below calculator inputs
